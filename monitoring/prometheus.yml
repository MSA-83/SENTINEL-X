global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "sentinel-x"
    static_configs:
      - targets: ["backend:8000"]
        labels:
          service: "sentinel-x"

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]